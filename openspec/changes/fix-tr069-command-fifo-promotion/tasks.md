## 1. 队列推进核心

- [x] 1.1 先增加失败测试，覆盖同设备上一条终态后只推进最早 `QUEUED`、活动头阻塞、并发幂等及 Redis 失败保留数据库状态
- [x] 1.2 实现事务化队列推进器，设置 `WAITING_DEVICE` 等待时间、截止时间和状态事件，并在提交后写入 Redis 唤醒令牌

## 2. 生命周期接线与恢复

- [x] 2.1 将推进器接入完成、失败、超时生产路径，并增加“上一条完成后自动执行下一条”的 FIFO 回归测试
- [x] 2.2 服务启动时恢复孤立 `QUEUED` 队首并重新唤醒已有 `WAITING_DEVICE`，增加恢复测试

## 3. 运行数据与验证

- [ ] 3.1 删除 `8CE468-SNB1234567892` 较新的重复 `GetRPCMethods`，保留一条命令继续执行
- [ ] 3.2 运行 TR-069 后端测试，重启 GVA，并验证保留命令被推进、唤醒且不再永久排队
